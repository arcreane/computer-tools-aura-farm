import pygame
from pygame.locals import*
import json
from save_manager import SaveManager
save_manager = SaveManager()

game_data = {                                                 #Data of the game
    "level": {1},
    "position": {"x","y"},
}

save_manager.save_game("save1.json", game_data)                #Save data

print("Disponible savingd :", save_manager.list_saves())       #List of saves
